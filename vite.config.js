import { defineConfig } from 'vite'

export default defineConfig({
  // Repositório atual: autojsa43-dev/autoflux-react
  // Se usar domínio próprio depois, troque para '/'
  base: '/autoflux-react/',
})
